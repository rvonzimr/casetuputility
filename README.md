100% Over-developed way to use the CA Setup utility.

Patches chdir so the program doesn't try writing to a data folder alongside the binary.

Tries to patch the QT theme so that the UI is usuable as well.
